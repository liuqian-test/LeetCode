#include <stdio.h>
#include <string.h>
int main(){
int n;
	char array1[n];
	string s, temp;
	scanf("%d", &n);
	for(int i = 0; i < n; i++){
	scanf("%s", &s);	
		if(s.size() > temp.size()){
		 temp = s;
		
		}
	}
	printf("%s",temp);

}
